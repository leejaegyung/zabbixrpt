# 03. 개발 작업분해 (WBS)

> 마일스톤은 [01-기획-PRD.md](./01-기획-PRD.md) §8과 정렬. 각 작업 완료 시 체크.

## Phase 0 — 프로젝트 스캐폴딩 ✅
- [x] `zabbix-reporter/` 모노레포 구조 생성 (backend / frontend / docker)
- [x] Laravel **12** 설치(PHP 8.2), Vue 3 + Vite + Tailwind + Pinia 초기화 — 프런트 빌드 검증 완료
- [x] `.env.example`, README 골격
- [x] API 라우팅 배선 + 헬스/프록시 스텁 — `GET /api/health`, `POST /api/zabbix`(화이트리스트) 구동 검증
- [ ] 기존 HTML 회귀 기준 스크린샷 캡처(검증 입력용) → 검증 단계로 이연

## Phase 1 — Laravel 프록시 + 버전 자동감지 인증 (M1) ✅
- [x] `config/zabbix.php` + `.env` 바인딩 (URL/토큰)
- [x] `ZabbixClient::call()` — Bearer 1차 → 인증 오류 시 legacy `auth` 폴백
- [x] 에러 정규화 매핑 (NETWORK/CORS, 404, 401, NOT_JSON, AUTH_EXPIRED, NO_TOKEN)
- [x] `POST /api/zabbix` 컨트롤러 + 메서드 화이트리스트
- [x] 토큰 서버 보관(클라이언트 미노출) + 요청별 토큰 오버라이드 옵션
- [x] **테스트**: 인증 폴백/정규화 (PHPUnit, 6.0/7.0/8.0 모의 응답) — 13 passed / 31 assertions

## Phase 2 — Vue SPA 기능 포팅 (M2) ✅
- [x] Pinia stores: connection / data / viewSettings / advanced / ui
- [x] `api/zabbix.js` 프록시 래퍼 (url·token 요청별 전달)
- [x] `composables/useFormat.js` (formatItemValue, severity, 시간 변환) + **테스트 13**
- [x] `composables/useAnalysis.js` (7대 분석, calculateStoragePrediction 등) + **테스트 10**
- [x] 4개 조회 모드 (대시보드/호스트/문제/아이템) + 기간 변환 (data 스토어)
- [x] 패널: ApiSettings / AdvancedSettings (로고·워터마크·그래프크기, localStorage)
- [x] 뷰: HostsTable / ProblemsTable / ItemsGrid (필터 + 페이지네이션)
- [x] AnalysisSection + 7개 카드 (개별 토글)
- [x] 공통: AppModal(alert/confirm), Pagination, Icon, ErrorBoundary, 토큰 도움말
- [x] 프로필 JSON 내보내기/불러오기 (토큰 제외 — 보안)
- [x] 가상(Mock) 데이터 로드
- [x] 화면 표시 설정 모달 (섹션·분석 카드·PDF 포함 토글)
- [x] 통합 스모크 테스트 3 (App 마운트·렌더 검증) — 전체 **26 tests** 통과
- 비고: 보안 개선 — 토큰을 sessionStorage(비영속)로 보관, 프로필에서 토큰 제외. 버그 수정 — 원본에서 렌더되지 않던 alert/confirm·토큰도움말 모달을 정상 동작화.

## Phase 3 — 클라이언트 PDF + 동등성 (M3) ✅
- [x] `usePdfExport.js` — html2canvas(scale 1.5, windowWidth 1000, bg #fff) → jsPDF A4 세로 페이지 분할, 파일명 `Zabbix_Report_{dataType}_{date}.pdf`
- [x] `ReportPreview.vue` + `PdfPage.vue` — `.pdf-page` A4(1000×1414) DOM. 분석/호스트/문제/아이템을 선택 항목 기준 페이지 분할
- [x] 워터마크/로고/그래프크기/분석포함 토글 출력 반영 (AnalysisSection `pdf` 모드 재사용 = 로직 단일 소스)
- [x] 라이브러리 번들 동봉(동적 import 코드분할) → CDN 의존 0. 데이터 0건·로딩 중 버튼 비활성
- [x] **테스트**: PDF 페이지 분할 4종 (medium 6p / large 8p / 선택필터 / 분석토글) — 전체 **30 tests**
- [x] 150건 초과 시 대량 추출 경고 confirm + 빈 선택 시 전체 추출 로직 이전
- [ ] **육안 검증**: 실제 브라우저에서 PDF 생성 → 기존 HTML과 픽셀 비교 (검증 단계, 실 브라우저/Zabbix 필요)
- 비고: 개선 — 원본은 CDN 라이브러리 로드 실패 시 버튼 비활성이었으나, 이제 번들 동봉으로 항상 가용(폐쇄망 안정성↑).

## Phase 4 — Docker 패키징 + 폐쇄망 (M4) ✅
- [x] 멀티스테이지 Dockerfile — php(composer→php-fpm-alpine) / nginx(node build→nginx)
- [x] `docker-compose.yml` (web nginx + app php-fpm), `.dockerignore`, entrypoint.sh
- [x] nginx 설정 (SPA fallback + `/api`·`/up` fastcgi 프록시)
- [x] `docker compose config` 검증 통과
- [x] README: 빌드/실행/환경변수 문서화
- [x] **이미지 빌드 성공** (app 174MB / web 94MB) + `docker compose up` 구동 (web :8090)
- [x] **E2E 검증**: SPA 서빙·#app 마운트·/api/health·화이트리스트 차단·/up·SPA fallback 전부 통과
- [x] **폐쇄망 런타임 검증**: index.html 외부 CDN 참조 0(원본엔 tailwindcss/unpkg/cdnjs 존재), PDF 라이브러리(html2canvas/jspdf) 로컬 청크 HTTP 200 서빙, 런타임 외부 CDN 호출 0
- 비고: 외부 참조는 mock 아이템 그래프의 `placehold.co`뿐(원본 동작 동일). 실 그래프는 사용자 Zabbix `chart.php`(의도된 동작).

## 완료 기준 (Definition of Done)
- 각 Phase의 테스트 통과 + [04-검증-회귀체크리스트.md](./04-검증-회귀체크리스트.md) 해당 항목 통과.
- 핵심 로직 커버리지 ≥ 70%.
